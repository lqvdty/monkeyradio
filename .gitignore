# macOS / design-tool metadata
.DS_Store
.thumbnail
**/.DS_Store
**/.thumbnail

# Export archives
*.zip

# Archived design-canvas source + exports (not part of the live app)
_archive/Monkey Radio India webapp design/

# Firebase
.firebase/
firebase-debug.log
*-debug.log

# Node
node_modules/

# Prerendered per-show social-preview pages + sitemap.xml + the crawlable
# hub pages, llms.txt and the prebuilt archive index (scripts/build-show-
# pages.mjs). Regenerated on every deploy via the firebase.json predeploy
# hook and CI.
/show/
/sitemap.xml
/archive.html
/selectors.html
/llms.txt
/assets/archive.json

# Compiled app bundle (scripts/build-app.mjs, from src/app.jsx). Regenerated
# the same way - see the firebase.json predeploy hook and CI.
/assets/app.js
